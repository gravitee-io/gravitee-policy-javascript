// Convert content to base64 and store in attributes
message.attributes.wronglyBase64EncodedContent = message.content
message.attributes.goodBase64Content = message.contentAsBase64;
message.attributes.byteArray = message.contentAsByteArray;
